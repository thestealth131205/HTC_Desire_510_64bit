# Warning: autogenerated
obj-m := commkm.o
commkm-objs := 	 check_kconfig.o comm_ev_kernel.o comm.o comm_os_linux.o comm_os_mod_linux.o comm_svc.o comm_transp_mvp.o

ccflags-$(CONFIG_ARM) += -march=armv7-a -mfpu=neon -mapcs -mno-sched-prolog
ccflags-$(CONFIG_ARM64) += -march=armv8-a
ccflags-y += -fno-pic -fno-dwarf2-cfi-asm -D__linux__
ccflags-y += -DCOMM_BUILDING_SERVER
ccflags-y += -DIN_MODULE -DGPLED_CODE
ccflags-y += --std=gnu89 -O2 -g2 -ggdb -fno-optimize-sibling-calls
ccflags-$(CONFIG_VMWARE_MVP_DEBUG) += -DMVP_DEBUG
ccflags-$(CONFIG_VMWARE_PVTCP_DEBUG) += -DDEBUG
